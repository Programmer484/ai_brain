[pytest]
addopts = -p no:launch_testing
testpaths = tests
markers =
    mock_ai: Mock AI/OpenAI calls, use real database
    mock_db: Mock database calls, use real AI
    mock_all: Mock everything (fastest tests) 